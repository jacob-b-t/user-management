import { RoleFormatterPipe } from './role-formatter.pipe';

describe('RoleFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
